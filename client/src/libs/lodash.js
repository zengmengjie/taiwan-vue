import _ from 'lodash'

export default {
  clone: _.clone,
  cloneDeep: _.cloneDeep,
  debounce: _.debounce,
  throttle: _.throttle,
  find: _.find,
  values: _.values,
  size: _.size,
  pick: _.pick,
  isEmpty: _.isEmpty,
  isFunction: _.isFunction,
  isArray: _.isArray,
  isSet: _.isSet,
  isDate: _.isDate,
  ceil: _.ceil,
  random: _.random
}
